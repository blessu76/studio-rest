> [!WARNING]
> **이 레포지토리는 더 이상 사용되지 않습니다 (Deprecated)**
>
> `idle.ipe.rest` 서비스는 **[`blessu76/idle-rest`](https://github.com/blessu76/idle-rest)** 레포로 이전되었습니다.
> 이 레포는 보존 목적으로만 유지되며 추가 업데이트는 이루어지지 않습니다.
>
> - **이전된 레포**: https://github.com/blessu76/idle-rest
> - **서비스 URL**: https://idle.ipe.rest
> - **이전 일자**: 2026-08-04

---

# [DEPRECATED] studio-rest → idle-rest로 이전됨

이 레포는 `idle.ipe.rest` (구 `idle()` 음악 플레이어 랜딩)를 서빙하던 레포였으나,
**2026-08-04** 기준으로 [`blessu76/idle-rest`](https://github.com/blessu76/idle-rest)로 이전 완료되었습니다.

**현재 이 레포는 아카이브 목적으로만 존재하며 배포에 사용되지 않습니다.**

## 이전 내역

| 항목 | 내용 |
|---|---|
| **구 레포** | `blessu76/studio-rest` (이 레포) |
| **신 레포** | [`blessu76/idle-rest`](https://github.com/blessu76/idle-rest) |
| **서비스 도메인** | `idle.ipe.rest` |
| **이전 일자** | 2026-08-04 |
| **이전 이유** | 레포명 정합성 확보 — `idle-rest`가 `idle.ipe.rest` 서브도메인과 일치 |

## 원래 README (참고용)

> AI 로파이 · 힐링 음악 브랜드 `ipe()` ("idle process")의 공식 홈페이지.
> *when the process finally rests*

단일 파일 정적 사이트. 빌드 과정이 없고 `index.html` 하나로 동작합니다.

---

*ipe(); // be still*